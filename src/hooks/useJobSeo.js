import { useEffect } from "react";
export default function useJobSeo(job){useEffect(()=>{if(!job)return;const title=`${job.title} Jobs in ${job.country} | Trans Arabian`;const description=`Apply for ${job.title} in ${job.country}. ${job.salary}, ${job.vacancies} vacancies. Licensed overseas recruitment by Trans Arabian.`;document.title=title;const set=(selector,key,keyValue,value)=>{let tag=document.querySelector(selector);if(!tag){tag=document.createElement("meta");tag.setAttribute(key,keyValue);document.head.appendChild(tag)}tag.setAttribute("content",value)};set('meta[name="description"]',"name","description",description);set('meta[property="og:title"]',"property","og:title",title);set('meta[property="og:description"]',"property","og:description",description);set('meta[property="og:image"]',"property","og:image",job.poster);let canonical=document.querySelector('link[rel="canonical"]');if(!canonical){canonical=document.createElement("link");canonical.rel="canonical";document.head.appendChild(canonical)}canonical.href=window.location.href.split("#")[0];return()=>{document.title="Trans Arabian Travel & Trade"}},[job])}
