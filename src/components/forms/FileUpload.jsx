import { useState } from "react";
const MAX_SIZE=5*1024*1024;const TYPES=["application/pdf","image/jpeg","image/png","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document"];
export default function FileUpload({name,label,accept=".pdf,.jpg,.jpeg,.png,.doc,.docx",required=false,onError}){const [fileName,setFileName]=useState("");return <label className="document-upload"><input type="file" name={name} accept={accept} required={required} onChange={e=>{const file=e.target.files[0];if(!file)return setFileName("");if(file.size>MAX_SIZE||!TYPES.includes(file.type)){e.target.value="";setFileName("");onError(`${label}: use PDF, JPG, PNG, DOC or DOCX files up to 5 MB.`);return}setFileName(file.name);onError("")}}/><span>⇧</span><div><strong>{label}{required?" *":""}</strong><small>{fileName||"PDF, JPG, PNG, DOC or DOCX · Maximum 5 MB"}</small></div></label>}
