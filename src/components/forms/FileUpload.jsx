import { useState } from "react";
const DEFAULT_TYPES=["application/pdf","image/jpeg","image/png","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document"];
export default function FileUpload({name,label,accept=".pdf,.jpg,.jpeg,.png,.doc,.docx",required=false,onError,allowedTypes=DEFAULT_TYPES,maxSize=5*1024*1024,help="PDF, JPG, PNG, DOC or DOCX · Maximum 5 MB"}){const [fileName,setFileName]=useState("");return <label className="document-upload"><input type="file" name={name} accept={accept} required={required} onChange={e=>{const file=e.target.files[0];if(!file)return setFileName("");if(file.size>maxSize||!allowedTypes.includes(file.type)){e.target.value="";setFileName("");onError(`${label}: ${help}.`);return}setFileName(file.name);onError("")}}/><span>⇧</span><div><strong>{label}{required?" *":""}</strong><small>{fileName||help}</small></div></label>}
