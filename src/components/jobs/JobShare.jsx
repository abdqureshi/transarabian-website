import { useState } from "react";
export default function JobShare({job,compact=false}){const [copied,setCopied]=useState(false);const url=`${window.location.origin}/jobs/${job.slug}`;const text=`${job.title} | ${job.country} | ${job.salary} | ${job.vacancies} vacancies`;const encoded=encodeURIComponent(`${text}\n${url}`);const copy=async()=>{await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1500)};return <div className={`job-share ${compact?"compact":""}`}><a href={`https://wa.me/?text=${encoded}`} target="_blank" rel="noreferrer" aria-label="Share on WhatsApp">WA</a><a href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`} target="_blank" rel="noreferrer" aria-label="Share on Facebook">f</a><a href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`} target="_blank" rel="noreferrer" aria-label="Share on LinkedIn">in</a><button onClick={copy} type="button">{copied?"Copied":"Copy link"}</button></div>}
