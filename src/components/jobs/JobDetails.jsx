import { Link } from "react-router-dom";
import BenefitBadge from "./BenefitBadge";import JobBadge from "./JobBadge";import JobGallery from "./JobGallery";import JobShare from "./JobShare";import RelatedJobs from "./RelatedJobs";import { openForApplications } from "../../data/jobRepository";
export default function JobDetails({job,jobs}){const open=openForApplications(job);const phone=job.applicationWhatsApp.replace(/\D/g,"").replace(/^0/,"92");return <><section className="job-detail-hero"><div className="job-detail-breadcrumb"><Link to="/jobs">Current Jobs</Link><span>›</span><b>{job.title}</b></div><div className="job-detail-title"><span className="job-detail-flag">{job.flag}</span><div><div className="detail-badges"><JobBadge status={job.status}/>{job.featured&&<span className="featured-badge">Featured</span>}</div><h1>{job.title}</h1><p>{job.country} · {job.industry}</p></div></div><div className="detail-hero-meta"><div><small>Salary</small><strong>{job.salary}</strong></div><div><small>Vacancies</small><strong>{job.vacancies}</strong></div><div><small>Experience</small><strong>{job.experience}</strong></div><div><small>Contract</small><strong>{job.contractType}</strong></div></div></section><section className="section job-detail-layout"><article className="job-detail-content"><section><div className="detail-heading"><span>About the role</span><h2>Job Description</h2></div><p>{job.description}</p></section><section><div className="detail-heading"><span>What you need</span><h2>Requirements</h2></div><ul>{job.requirements.map(item=><li key={item}>{item}</li>)}</ul></section><section><div className="detail-heading"><span>Education</span><h2>Qualifications</h2></div><p>{job.qualification}</p></section><section><div className="detail-heading"><span>What is included</span><h2>Benefits</h2></div><div className="benefit-row large">{job.benefits.map(item=><BenefitBadge key={item}>{item}</BenefitBadge>)}</div></section><JobGallery job={job}/><section className="application-instructions"><div className="detail-heading"><span>How to apply</span><h2>Application Instructions</h2></div><p>{job.instructions}</p><p>Email: <a href={`mailto:${job.applicationEmail}`}>{job.applicationEmail}</a></p><div>{open?<Link className="button" to={`/jobs/${job.slug}/apply`}>Apply Online →</Link>:<span className="button disabled">Applications Closed</span>}<a className="button whatsapp-button" href={`https://wa.me/${phone}?text=${encodeURIComponent(`I want to apply for ${job.title} in ${job.country}`)}`} target="_blank" rel="noreferrer">Apply by WhatsApp</a><a className="button button-ghost" href={`mailto:${job.applicationEmail}?subject=Application: ${job.title}`}>Apply by Email</a></div><JobShare job={job}/></section></article><aside className="job-detail-sidebar"><div><h3>Job Overview</h3><dl><div><dt>Country</dt><dd>{job.country}</dd></div><div><dt>Industry</dt><dd>{job.industry}</dd></div><div><dt>Category</dt><dd>{job.category}</dd></div><div><dt>Vacancies</dt><dd>{job.vacancies}</dd></div><div><dt>Contract</dt><dd>{job.contractType}</dd></div><div><dt>Working hours</dt><dd>{job.workingHours}</dd></div><div><dt>Permission no.</dt><dd>{job.permissionNumber}</dd></div><div><dt>Posted</dt><dd>{job.postedDate}</dd></div><div><dt>Closing</dt><dd>{job.closingDate||"Open until filled"}</dd></div></dl>{open?<Link className="button" to={`/jobs/${job.slug}/apply`}>Apply Now →</Link>:<span className="button disabled">Applications Closed</span>}</div><p>Recruitment is managed by Trans Arabian Travel & Trade, a licensed Overseas Employment Promoter.</p></aside></section>{open&&<Link className="mobile-sticky-apply" to={`/jobs/${job.slug}/apply`}>Apply Now →</Link>}<RelatedJobs jobs={jobs} current={job}/></>}
