import { useState } from "react";
export default function JobGallery({job}){const [open,setOpen]=useState(false);return <section className="job-poster-section"><div className="detail-heading"><span>Original advertisement</span><h2>Recruitment poster</h2></div><button className="job-poster" onClick={()=>setOpen(true)}><img loading="lazy" src={job.poster} alt={`${job.title} recruitment poster`}/><span>View poster ↗</span></button><div className="poster-actions"><a className="button button-ghost small" href={job.poster} download target="_blank" rel="noreferrer">Download Advertisement ↓</a></div><p className="poster-note">Replace the poster field in jobs.json with the approved original advertisement when available.</p>{open&&<div className="lightbox" role="dialog" aria-modal="true" onClick={()=>setOpen(false)}><button aria-label="Close">×</button><figure onClick={e=>e.stopPropagation()}><img src={job.poster} alt={`${job.title} recruitment poster`}/><figcaption>{job.title} — {job.country}</figcaption></figure></div>}</section>}
