const unique=(jobs,key)=>[...new Set(jobs.map(job=>job[key]))].sort();
export default function JobFilters({jobs,filters,onChange,onApply,onReset}){const filter=(key,label,values)=><label><span>{label}</span><select value={filters[key]} onChange={e=>onChange(key,e.target.value)}><option value="All">All</option>{values.map(v=><option key={v}>{v}</option>)}</select></label>;return <details className="portal-filters reveal" open><summary>Filter vacancies <span>＋</span></summary><div className="filter-selects">{filter("country","Country",unique(jobs,"country"))}{filter("industry","Industry",unique(jobs,"industry"))}{filter("category","Category",unique(jobs,"category"))}{filter("experience","Experience",unique(jobs,"experience"))}{filter("salary","Salary range",unique(jobs,"salaryBand"))}{filter("contractType","Contract type",unique(jobs,"contractType"))}{filter("status","Status",unique(jobs,"status"))}<label className="featured-filter"><span>Featured jobs</span><input type="checkbox" checked={filters.featured} onChange={e=>onChange("featured",e.target.checked)}/><b>Featured only</b></label></div><div className="filter-actions"><button type="button" onClick={onReset}>Clear Filters</button><button className="button small" type="button" onClick={onApply}>Apply Filters</button></div></details>}
