export default function DataTable({columns,rows,keyField="id",empty="No records found",selected=[],onSelect}){return <div className="data-table-wrap"><table className="data-table"><thead><tr>{onSelect&&<th><input type="checkbox" aria-label="Select all visible" onChange={e=>onSelect(e.target.checked?rows.map(r=>r[keyField]):[])}/></th>}{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.length?rows.map(row=><tr key={row[keyField]}>{onSelect&&<td><input type="checkbox" checked={selected.includes(row[keyField])} onChange={()=>onSelect(selected.includes(row[keyField])?selected.filter(id=>id!==row[keyField]):[...selected,row[keyField]])}/></td>}{columns.map(c=><td key={c.key}>{c.render?c.render(row):row[c.key]??"—"}</td>)}</tr>):<tr><td className="table-empty" colSpan={columns.length+(onSelect?1:0)}>{empty}</td></tr>}</tbody></table></div>}
