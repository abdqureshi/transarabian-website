export default function Pagination({page,pages,onChange}){if(pages<2)return null;return <nav className="pagination" aria-label="Results pages"><button disabled={page===1} onClick={()=>onChange(page-1)}>←</button>{Array.from({length:pages},(_,i)=>i+1).map(n=><button className={n===page?"active":""} onClick={()=>onChange(n)} key={n}>{n}</button>)}<button disabled={page===pages} onClick={()=>onChange(page+1)}>→</button></nav>}
