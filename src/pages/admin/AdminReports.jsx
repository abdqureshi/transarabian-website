import { useEffect,useState } from "react";import { reportsService } from "../../services/reportsService";import LoadingSpinner from "../../components/common/LoadingSpinner";import { formatStatus } from "../../config/statuses";
export default function AdminReports(){const [data,setData]=useState(null);useEffect(()=>{reportsService.summary().then(setData)},[]);if(!data)return <LoadingSpinner label="Building reports"/>;const groups=data.applications.reduce((all,item)=>({...all,[item.status]:(all[item.status]||0)+1}),{});return <><header className="ats-page-head"><div><span>Recruitment intelligence</span><h1>Reports</h1><p>Operational summaries and export-ready recruitment data.</p></div><button onClick={()=>reportsService.download(reportsService.toCsv(data.applications,[{key:"id",label:"ID"},{key:"status",label:"Status"},{key:"submitted_at",label:"Submitted"}]),"application-report.csv")}>Export CSV ↓</button></header><div className="report-grid"><section><h2>Applications by Status</h2>{Object.entries(groups).map(([key,value])=><div className="report-bar" key={key}><span>{formatStatus(key)}</span><i style={{width:`${Math.max(5,value/Math.max(1,data.applications.length)*100)}%`}}></i><b>{value}</b></div>)}</section><section><h2>Core Metrics</h2><dl><div><dt>Total candidates</dt><dd>{data.candidateCount}</dd></div><div><dt>Total applications</dt><dd>{data.applications.length}</dd></div><div><dt>Active jobs</dt><dd>{data.jobs.filter(j=>j.status==="open").length}</dd></div><div><dt>Scheduled interviews</dt><dd>{data.interviews.filter(i=>i.attendance_status==="scheduled"||i.status==="Scheduled").length}</dd></div></dl></section></div></>}
