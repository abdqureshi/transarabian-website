export default function AdminSettings(){return <><header className="ats-page-head"><div><span>System configuration</span><h1>Settings</h1><p>Provider secrets are configured only in Supabase Edge Function secrets.</p></div></header><div className="settings-grid"><article><h2>Email Provider</h2><p>Configure Resend using <code>RESEND_API_KEY</code>, <code>EMAIL_FROM</code>, and <code>RECRUITMENT_EMAIL</code>.</p><span>Edge Function: send-email</span></article><article><h2>WhatsApp Business</h2><p>Configure Meta Cloud API with an approved business number and template names.</p><span>Edge Function: send-whatsapp</span></article><article><h2>Data & Storage</h2><p>Candidate files remain private and are served through short-lived signed URLs.</p><span>Bucket: candidate-documents</span></article><article><h2>Audit & Retention</h2><p>Protected record changes are logged. Configure retention periods according to legal requirements.</p><span>Table: audit_logs</span></article></div></>}
