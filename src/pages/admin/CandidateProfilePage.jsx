import { useEffect,useState } from "react";import { useParams } from "react-router-dom";import { candidatesService } from "../../services/candidatesService";import DocumentsPanel from "../../components/candidates/DocumentsPanel";import LoadingSpinner from "../../components/common/LoadingSpinner";import StatusBadge from "../../components/admin/StatusBadge";
export default function CandidateProfilePage(){const {id}=useParams();const [candidate,setCandidate]=useState(null);useEffect(()=>{candidatesService.get(id).then(setCandidate)},[id]);if(!candidate)return <LoadingSpinner label="Loading candidate"/>;return <><header className="ats-page-head"><div><span>Candidate profile</span><h1>{candidate.full_name}</h1><p>{candidate.current_trade} · {candidate.city}</p></div>{candidate.duplicate_flags?.length>0&&<span className="duplicate-warning">Possible duplicate</span>}</header><div className="candidate-profile-grid"><section className="review-card"><h2>Personal & Passport</h2><dl className="profile-facts">{Object.entries({"Father’s name":candidate.father_name,CNIC:candidate.cnic,"Date of birth":candidate.date_of_birth,Phone:candidate.phone,WhatsApp:candidate.whatsapp,Email:candidate.email,Address:candidate.current_address,Passport:candidate.passport_number,"Passport expiry":candidate.passport_expiry_date,"Passport status":candidate.passport_status}).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v||"—"}</dd></div>)}</dl></section><section className="review-card"><h2>Professional</h2><dl className="profile-facts">{Object.entries({Trade:candidate.current_trade,"Total experience":candidate.total_experience,"Gulf experience":candidate.gulf_experience,"Saudi experience":candidate.saudi_experience,Qualification:candidate.highest_qualification,"Technical qualification":candidate.technical_qualification,Availability:candidate.availability,"Preferred country":candidate.preferred_country}).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v||"—"}</dd></div>)}</dl></section><DocumentsPanel documents={candidate.candidate_documents}/><section className="review-card"><h2>Applications</h2>{candidate.applications?.map(app=><article className="profile-application" key={app.id}><div><b>{app.job_title}</b><small>{app.reference_number}</small></div><StatusBadge status={app.status}/></article>)}</section></div></>}
