import { useEffect,useState } from "react";import { reportsService } from "../../services/reportsService";import LoadingSpinner from "../../components/common/LoadingSpinner";import { formatStatus } from "../../config/statuses";
const pipeline=["submitted","under_review","shortlisted","interview_scheduled","selected","deployed"];
export default function AdminDashboard(){const [data,setData]=useState(null);useEffect(()=>{reportsService.summary().then(setData).catch(()=>setData({jobs:[],candidateCount:0,applications:[],interviews:[]}))},[]);if(!data)return <LoadingSpinner label="Loading dashboard"/>;const today=new Date().toISOString().slice(0,10);const cards=[[data.jobs.filter(j=>j.status==="open").length,"Active Jobs"],[data.candidateCount,"Candidates"],[data.applications.length,"Applications"],[data.applications.filter(a=>a.submitted_at?.startsWith(today)).length,"Applications Today"],[data.applications.filter(a=>a.status==="shortlisted").length,"Shortlisted"],[data.interviews.filter(i=>i.attendance_status==="scheduled"||i.status==="Scheduled").length,"Interviews"],[data.applications.filter(a=>a.status==="medical_pending").length,"Medical Pending"],[data.applications.filter(a=>a.status==="visa_processing").length,"Visa Processing"]];return <><header className="ats-page-head"><div><span>Recruitment operations</span><h1>Dashboard</h1><p>Live overview of the recruitment pipeline.</p></div></header><div className="ats-stat-grid">{cards.map(([value,label])=><article key={label}><strong>{value}</strong><span>{label}</span></article>)}</div><div className="ats-dashboard-panels"><section><h2>Pipeline Summary</h2>{pipeline.map(status=><div className="pipeline-row" key={status}><span>{formatStatus(status)}</span><b>{data.applications.filter(a=>a.status===status).length}</b></div>)}</section><section><h2>Operational Attention</h2><p>Use Applications to review new submissions, assign recruiters and update statuses.</p><p>Use Candidates to locate talent by identity, trade, location and experience.</p><p>Every protected change is captured in the audit log by the database.</p></section></div></>}
