# Recruitment Admin — Phase 2

Reserved module boundary for protected job management and application review. Future routes include `/admin/jobs`, `/admin/jobs/new`, `/admin/jobs/:id/edit`, `/admin/applications`, and `/admin/applications/:id`. These routes must only be registered after server-side authentication, role authorization, private file storage and audit logging are implemented.
