const shell=(title:string,body:string)=>`<!doctype html><html><body style="margin:0;background:#f5f7fa;font-family:Arial,sans-serif;color:#061b33"><table width="100%" cellpadding="0" cellspacing="0"><tr><td style="padding:30px"><table width="600" align="center" style="max-width:100%;background:#fff;border-top:5px solid #f4b400"><tr><td style="padding:32px"><div style="font-size:20px;font-weight:bold">TRANS ARABIAN</div><p style="color:#9a7100;font-size:11px;text-transform:uppercase">Travel & Trade · Since 1974</p><h1 style="font-size:24px">${title}</h1>${body}<hr style="border:0;border-top:1px solid #e3e8ed;margin:25px 0"><p style="font-size:11px;color:#68788a">Trans Arabian Travel & Trade<br>23, I & T Centre, G-7/1, Islamabad</p></td></tr></table></td></tr></table></body></html>`;
export const emailTemplates:Record<string,(v:Record<string,string>)=>{subject:string;html:string}>={application_received:v=>({subject:`Application received — ${v.reference}`,html:shell("Application received",`<p>Dear ${v.name},</p><p>Your application for <b>${v.jobTitle}</b> has been received.</p><p>Reference: <b>${v.reference}</b></p><p>Our recruitment team will contact you if shortlisted.</p>`)}),status_update:v=>({subject:`Application update — ${v.reference}`,html:shell("Application status update",`<p>Dear ${v.name},</p><p>Your application for <b>${v.jobTitle}</b> is now <b>${v.status}</b>.</p><p>${v.nextSteps||"Our team will contact you when action is required."}</p>`)}),interview_scheduled:v=>({subject:`Interview scheduled — ${v.reference}`,html:shell("Interview scheduled",`<p>Dear ${v.name},</p><p>Your interview is scheduled for <b>${v.date}</b> at <b>${v.location}</b>.</p><p>${v.instructions||"Bring original documents."}</p>`)})};
